#:kivy 1.10.1


<Playground>:
  snake: snake_id
  fruit: fruit_id
  death: death_id

  Snake:
    id: snake_id
    width: root.width / root.col_number
    height: root.height / root.row_number

  Fruit:
    id: fruit_id
    width: root.width / root.col_number
    height: root.height / root.row_number

  Death:
    id: death_id
    width: root.width / root.col_number
    height: root.height / root.row_number

  Label:
    font_size: 70
    center_x: root.x + root.width / root.col_number * 2
    top: root.top - root.height / root.row_number
    text: str(root.score)

<Snake>:
  head: snake_head_id
  tail: snake_tail_id

  SnakeHead:
    id: snake_head_id
    width: root.width
    height: root.height

  SnakeTail:
    id: snake_tail_id
    width: root.width
    height: root.height

<PlaygroundScreen>:
  game_engine: playground_widget_id

  Playground:
    id: playground_widget_id

<WelcomeScreen>:
  AnchorLayout:
    anchor: "center"

  BoxLayout:
    orientation: "vertical"
    size_hint: (0.5, 1)
    spacing: 10

    Label:
      size_hint_y: .4
      text: "JHEBOI's Snake"
      valign: "bottom"
      bold: True
      font_size: 50
      padding: 0, 0

    AnchorLayout:
      anchor_x: "center"
      size_hint_y: .6

      BoxLayout:
        size_hint: .5, .5
        orientation: "vertical"
        spacing: 10

        Button:
          halign: "center"
          valign: "middle"
          text: "Play"
          on_press: root.manager.current = "playground_screen"

        # No Option Buttons for now. We will add them in later.
        #Button:
          #halign: "center"
          #valign: "middle"
          #text: "Options"
          #on_press: root.show_popup()